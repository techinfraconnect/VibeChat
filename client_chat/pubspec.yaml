name: client_chat
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  flutter_webrtc: ^0.12.0
  firebase_core: ^3.15.2
  firebase_database: ^11.1.4
  firebase_messaging: ^15.2.4  # Added for Push Notifications
  flutter_callkit_incoming: ^3.1.3
  uuid: ^4.3.3
  flutter_local_notifications: ^17.2.0  # Added for local alert handling
  socket_io_client: ^2.0.3 
  shared_preferences: ^2.3.2
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true