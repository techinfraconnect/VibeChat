workflows:
  ios-unsigned-workflow:
    name: iOS Unsigned Build
    max_build_duration: 60
    working_directory: admin_chat
    environment:
      flutter: stable
      xcode: latest
    scripts:
      - name: Build Unsigned iOS App
        script: |
          flutter create . --platforms=ios
          flutter clean
          flutter pub get
          # Build for simulator/generic device architecture without archive signing checks
          flutter build ios --simulator --release
    artifacts:
      - build/ios/iphonesimulator/*.app